---
action:
  exec:
    args: 
      - az
      - spring
      - app
      - delete
      - --resource-group
      - '{{resource-group}}'
      - --service
      - 'si-{{user-name}}'
      - --name 
      - '{{project-name}}'      
---