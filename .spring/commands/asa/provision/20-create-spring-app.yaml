---
action:
  exec:
    args: 
      - az
      - spring
      - app
      - create
      - --resource-group
      - '{{resource-group}}'
      - --service
      - 'si-{{user-name}}'
      - --name 
      - '{{project-name}}'
      - --assign-endpoint
      - true
---